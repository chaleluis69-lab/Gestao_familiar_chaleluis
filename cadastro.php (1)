<?php 
include 'conexao.php'; 
$sql_pessoas = "SELECT id, nome, apelido_familiar FROM pessoas ORDER BY nome ASC";
$result_pessoas = $conn->query($sql_pessoas);
$pais = [];
$maes = [];

if ($result_pessoas->num_rows > 0) {
    while($row = $result_pessoas->fetch_assoc()) {
        $lista_geral[] = $row;
    }
}
?>
<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>Cadastro de Membro</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="estilo.css">
</head>
<body class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card-glass">
                <div class="mb-4">
                    <h2 class="fw-bold">Novo <span style="color: var(--primary)">Membro</span></h2>
                    <p class="text-secondary small text-uppercase">Vincular novo registo à árvore genealógica</p>
                </div>

                <form action="salvar.php" method="POST">
                    <div class="row g-3 mb-4">
                        <div class="col-md-12">
                            <label class="form-label text-secondary small">Nome Completo</label>
                            <input type="text" name="nome" class="form-control form-control-lg" required placeholder="Ex: Chale Luis">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label text-secondary small">Data de Nascimento</label>
                            <input type="date" name="data_nascimento" class="form-control" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label text-secondary small">Gênero</label>
                            <select name="sexo" class="form-select">
                                <option value="M">Masculino</option>
                                <option value="F">Feminino</option>
                            </select>
                        </div>
                    </div>

                    <div class="row g-3 mb-4">
                        <div class="col-md-6">
                            <label class="form-label text-secondary small">Nome do Pai</label>
                            <select name="id_pai" class="form-select">
                                <option value="">Não Registado</option>
                                <?php foreach($lista_geral as $p): ?>
                                    <option value="<?= $p['id'] ?>"><?= htmlspecialchars($p['nome']) ?></option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label text-secondary small">Nome da Mãe</label>
                            <select name="id_mae" class="form-select">
                                <option value="">Não Registada</option>
                                <?php foreach($lista_geral as $p): ?>
                                    <option value="<?= $p['id'] ?>"><?= htmlspecialchars($p['nome']) ?></option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                    </div>

                    <div class="mb-5">
                        <label class="form-label text-secondary small">Apelido Familiar</label>
                        <input type="text" name="apelido_familiar" class="form-control" placeholder="Ex: Família Mateus">
                    </div>

                    <div class="d-flex gap-3">
                        <button type="submit" class="btn-modern btn-primary-modern w-100 justify-content-center">Finalizar Cadastro</button>
                        <a href="listar.php" class="btn-modern btn-outline-modern text-decoration-none">Cancelar</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>